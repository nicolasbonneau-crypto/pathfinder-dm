[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=app --cov-report=term-missing --cov-report=lcov:coverage.lcov"

[tool.coverage.run]
omit = ["tests/*", "migrations/*"]
